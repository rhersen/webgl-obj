h1= title
p Welcome to Express
ol
  - each station in stations
    li
      a(href="departures/#{station.id}.html")
        #{station.id}
      a(href="station/#{station.id}")
        #{station.name}
